
server {
    listen 80;

    # location / {
    #         root /var/www/html;
    #         index index.html;
    #     }
    
    location /api1/ {
        proxy_pass http://web_app:3000/;
    }

}
